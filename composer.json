{
    "require": {
        "php": ">=8.1"
    },
    "scripts": {
        "post-install-cmd": [
            "apt-get update",
            "apt-get install -y python3 python3-pip qpdf wget curl --no-install-recommends",
            "pip3 install --upgrade pip",
            "pip3 install gdown --break-system-packages",
            "which qpdf || echo '❌ qpdf installation failed'",
            "qpdf --version || echo '❌ qpdf not working'"
        ]
    }
}
