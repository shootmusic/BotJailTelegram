{
    "require": {
        "php": ">=8.1"
    },
    "scripts": {
        "post-install-cmd": [
            "apt-get update && apt-get install -y qpdf python3-pip && pip3 install gdown"
        ]
    }
}
